# ============ TravelAgent 环境配置示例 ============
# 复制本文件为 .env 后按需修改（不配置 LLM Key 时系统自动运行在离线模板模式，功能完整可用）

# --- LLM 配置（二选一或都不填）---
# 不配置任何 Key：系统使用内置离线模板引擎，全程可运行（演示/测试）
# 配置 Key 后：Agent 自动升级为真实 LLM 推理（对话/解析/摘要等）

# 方式一：OpenAI
OPENAI_API_KEY=

# 方式二：DeepSeek（兼容 OpenAI 协议，推荐国内使用）
DEEPSEEK_API_KEY=

# 自定义 OpenAI 兼容网关（可选，例如 OneAPI / 通义兼容端点）
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-chat
# EMBEDDING_MODEL=text-embedding-3-small

# --- 服务参数 ---
# 天气：默认使用 Open-Meteo 免费接口（无需 Key），失败时自动降级为本地模拟数据
# WEATHER_CACHE_TTL=600
# HTTP_TIMEOUT=12

# --- 网页搜索（可选 Key）---
# 未配置时自动使用免费 DuckDuckGo / Bing 兜底；配置后优先使用对应 provider（质量更稳定）
# TAVILY_API_KEY=
# SERPAPI_KEY=

# --- 高德地图（可选 Key：真实酒店 POI / 地理编码）---
# 申请：https://console.amap.com/dev/key/app （类型选「Web服务」，个人开发者免费）
# 未配置时酒店推荐回退本地知识库数据；也可在「设置」页运行时填写，即时生效
# AMAP_API_KEY=

# --- 提示词覆盖（可选）---
# 所有提示词已集中在 backend/app/prompts.py；在此覆盖可改各智能体人设/指令，无需改代码。
# 留空则使用内置默认值。多行内容用引号包裹（如 "第一行\n第二行"）。
# PROMPT_SYSTEM=你是「旅行星球」平台的旅行规划总指挥...
# PROMPT_SUMMARY=请把对话压缩为一段要点摘要...
# PROMPT_PLANNER_PARSE=解析中文旅行需求，输出 JSON...
# PROMPT_FEEDBACK_LESSONS=根据用户旅行反馈生成改进经验...
# PROMPT_MEMORY_EXTRACT=从用户话语抽取旅行偏好与事实...
# PROMPT_MCP_INSTRUCTIONS=旅行规划智能体工具集...

# ===== TravelAgent 后端依赖 =====
# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
python-multipart>=0.0.9

# 配置 / 数据校验
pydantic>=2.7.0
pydantic-settings>=2.3.0

# 数据库（异步 SQLAlchemy + SQLite）
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0

# HTTP 客户端（异步调用外部 API）
httpx>=0.27.0

# 数值计算 / 向量
numpy>=1.26.0

# LLM（OpenAI 兼容接口，支持 DeepSeek / 通义等）
openai>=1.40.0

# 向量数据库（RAG 向量存储）
chromadb>=0.5.0

# BM25 稀疏检索（RAG 混合召回）
rank-bm25>=0.2.2

# MCP（Model Context Protocol）服务端 / 客户端 SDK
mcp>=1.9.0,<2.0.0
